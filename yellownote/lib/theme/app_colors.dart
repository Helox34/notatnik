import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const yellow = Color(0xFFFDB623);
  static const yellowDark = Color(0xFFE5A520);
  
  // Navy/Dark colors
  static const darkNavy = Color(0xFF1E2749);
  static const darkNavyLight = Color(0xFF2A3456);
  
  // Neutrals
  static const white = Color(0xFFFFFFFF);
  static const lightGray = Color(0xFFF5F5F5);
  static const mediumGray = Color(0xFFE0E0E0);
  static const darkGray = Color(0xFF757575);
  
  // Category Colors (pastel backgrounds)
  static const projectsBg = Color(0xFFFFF9E6); // Soft yellow
  static const diaryBg = Color(0xFFF3E5FF); // Soft purple
  static const listsBg = Color(0xFFE8F5E9); // Soft green
  static const notesBg = Color(0xFFE3F2FD); // Soft blue
  
  // Status Colors
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFFA726);
  
  // Dark mode colors
  static const darkBg = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkCard = Color(0xFF2C2C2C);
}
